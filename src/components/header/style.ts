import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  divider: {
    width: "100%",
    marginTop: 12,
    height: 1,
    alignSelf: "center",
  },
});

export default styles;
